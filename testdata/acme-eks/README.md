# ACME EKS production-like placeholders

Safe shapes only. Copy into a protected directory, replace `REPLACE_*`
values from a successful `metrum-fleetctl plan`, chmod `0600`, and never
commit filled copies.

See `docs/ACME_EKS_PRODUCTION_LIKE_DEPLOY.md` and issue #869.
