# Placeholder only. Replace every REPLACE_* value in a protected mode-0600 file.
# Never commit credentials, DSNs, tokens, or license payloads.
# Hostname is derived by the plan as <namespace>.<profile.hostname_suffix>
# (for ACME: expect acme.apps.metrum.ai once the protected profile suffix is apps.metrum.ai
# and the namespace is derived from customer_id/stage/profile policy).
api_version: metrum.ai/smartrouter-deployment/v1
customer_id: acme
stage: nonproduction
release: latest-approved
resource_profile: REPLACE_APPROVED_RESOURCE_PROFILE
state_profile: REPLACE_APPROVED_STATE_PROFILE
database_profile: REPLACE_APPROVED_DEDICATED_RDS_PROFILE
runtime_bundle_ref: aws-secretsmanager:///REPLACE_PROTECTED_ACME_RUNTIME_BUNDLE
config_revision: REPLACE_IMMUTABLE_CONFIG_REVISION
license:
  request_ref: aws-ssm:///REPLACE_PROTECTED_ACME_LICENSE_REQUEST
  validity: 168h
