# Required. Pin to the image tag loaded from the deployment package, for example
# metrum-ai-router-<version>-docker-linux-amd64 ships metrum-ai-router:<version>-linux-amd64.
METRUM_AI_ROUTER_VERSION=replace-with-package-version-linux-amd64

ROUTER_HOSTNAME=router.example.com
CADDY_EMAIL=admin@example.com
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443

# PostgreSQL opt-in only. Include docker-compose.postgres-localhost.yml and
# configure server.usage_db.driver: postgres plus dsn: ${ROUTER_USAGE_DB_DSN}.
# POSTGRES_DB=llmrouter
# POSTGRES_USER=llmrouter
# POSTGRES_PASSWORD=replace-with-strong-random-db-password
# ROUTER_USAGE_DB_DSN=host=postgres port=5432 user=llmrouter password=replace-with-same-strong-random-db-password dbname=llmrouter sslmode=disable TimeZone=UTC
# POSTGRES_HOST_PORT=15432
