apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrum-ai-router
spec:
  template:
    spec:
      containers:
        - name: router
          image: registry.example.com/metrum-ai-router:<version>-linux-amd64
          # Router must not request nvidia.com/gpu; serving pods own accelerators.
