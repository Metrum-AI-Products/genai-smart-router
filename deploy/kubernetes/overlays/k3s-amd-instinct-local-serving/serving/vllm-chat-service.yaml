apiVersion: v1
kind: Service
metadata:
  name: vllm-chat
  namespace: smart-llmrouter
  labels:
    app.kubernetes.io/name: vllm-chat
    app.kubernetes.io/component: local-serving
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm-chat
  ports:
    - name: http
      port: 8000
      targetPort: http
