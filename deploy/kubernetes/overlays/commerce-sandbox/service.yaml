apiVersion: v1
kind: Service
metadata:
  name: commerce-purchase
  labels:
    app.kubernetes.io/name: commerce-purchase
spec:
  selector:
    app.kubernetes.io/name: commerce-purchase
  ports:
    - name: http
      port: 80
      targetPort: http
