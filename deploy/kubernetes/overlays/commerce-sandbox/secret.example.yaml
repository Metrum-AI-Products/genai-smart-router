# EXAMPLE ONLY — do not commit real Stripe keys.
# Create a live Secret out-of-band (for example kubectl create secret generic)
# with empty placeholders replaced by sk_test_ / whsec_ values from the Stripe dashboard.
apiVersion: v1
kind: Secret
metadata:
  name: commerce-stripe-example
  labels:
    app.kubernetes.io/name: smartrouter-commerce
type: Opaque
stringData:
  STRIPE_SECRET_KEY: ""
  STRIPE_PUBLISHABLE_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
