apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smart-llmrouter-state
  labels:
    app.kubernetes.io/name: smart-llmrouter
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
