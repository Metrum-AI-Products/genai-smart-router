apiVersion: v1
kind: Secret
metadata:
  name: smart-llmrouter-secrets
  labels:
    app.kubernetes.io/name: smart-llmrouter
type: Opaque
stringData:
  env.json: |
    {
      "OPENAI_API_KEY": "",
      "OPENROUTER_API_KEY": ""
    }
  license.json: |
    {
      "replace": "with operator-issued signed license JSON"
    }
